import Link from "next/link";
import type { Locale } from "@/types";
export function SiteFooter({locale}:{locale:Locale}) { const ar=locale==='ar'; return <footer className="dc-footer"><div><div className="dc-brand"><span>D<span>&</span>C</span><div><strong>Debit & Credit</strong><small>by Money Coder</small></div></div><p>{ar?'العب، اتعلم، تدرب، اثبت مهاراتك، وابنِ جاهزيتك المهنية.':'Play, learn, practice, prove skills, and build career readiness.'}</p><nav><Link href={`/${locale}/learning-map`}>{ar?'خريطة التعلم':'Learning Map'}</Link><Link href={`/${locale}/arena`}>{ar?'الساحة':'Arena'}</Link><Link href={`/${locale}/companies`}>{ar?'للشركات':'For companies'}</Link></nav></div></footer>; }
