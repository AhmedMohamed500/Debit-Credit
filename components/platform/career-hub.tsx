'use client';
import{useEffect,useState}from'react';import Link from'next/link';import{ArrowLeft,ArrowRight,BriefcaseBusiness,CheckCircle2,FileText,Lock,Map,ShieldCheck,Sparkles,Target}from'lucide-react';import{useGame}from'@/lib/campaign/store';import{firstShiftSkillEvidence,calculatePassport,calculateRoleReadiness}from'@/lib/career/evidence';import{roleCatalog,roleIds,skillCatalog}from'@/lib/career/catalog';import{BrowserCareerProfileRepository,createDefaultProfile}from'@/lib/career/repository';import type{RoleId}from'@/lib/career/model';import{recommendMission,roleSkillGroups,professionalRank}from'@/lib/platform';import type{Locale}from'@/types';import{PlatformNav}from'./platform-nav';
export function CareerHub({locale}:{locale:Locale}){const ar=locale==='ar',say=(en:string,a:string)=>ar?a:en,Arrow=ar?ArrowLeft:ArrowRight,{state}=useGame(),[roleId,setRoleId]=useState<RoleId>('junior-accountant');useEffect(()=>{setRoleId((new BrowserCareerProfileRepository().get()??createDefaultProfile(state.name)).targetRoleId)},[state.name]);const evidence=firstShiftSkillEvidence(state,'local-player'),passport=calculatePassport(evidence),rank=professionalRank(passport),readiness=calculateRoleReadiness(roleId,passport),groups=roleSkillGroups(roleId,passport),recommendation=recommendMission(roleId,passport),status=readiness.score===null?say('Getting Started','في البداية'):readiness.score>=75?say('Strong Evidence','أدلة قوية'):readiness.score>=55?say('Nearly Ready','قريب من الجاهزية'):say('Developing','قيد التطوير');return <main className="platform-page career-hub-page"><PlatformNav locale={locale}/><header className="career-hub-hero"><div><span><BriefcaseBusiness/>{say('CAREER HUB','مركز المسار المهني')}</span><h1>{say('Turn accounting work into career direction.','حوّل شغلك المحاسبي إلى اتجاه مهني.')}</h1><p>{say('Your role fit comes from explainable evidence, never XP, level, or leaderboard rank.','ملاءمة الدور تأتي من أدلة قابلة للتفسير، وليس XP أو مستوى اللعبة أو الترتيب.')}</p></div><aside><small>{say('TARGET ROLE','الدور المستهدف')}</small><select value={roleId} onChange={e=>setRoleId(e.target.value as RoleId)}>{roleIds.map(id=><option key={id} value={id}>{roleCatalog[id].label[locale]}</option>)}</select><strong>{readiness.score===null?'—':`${readiness.score}%`}</strong><b>{status} · {rank}</b></aside></header><section className="career-hub-grid"><article className="role-coverage"><header><Target/><div><small>{say('ROLE READINESS','جاهزية الدور')}</small><h2>{roleCatalog[roleId].label[locale]}</h2></div></header><div className="coverage-meter"><i><b style={{width:`${readiness.coverage}%`}}/></i><span>{readiness.coverage}% {say('relevant skill coverage','تغطية المهارات المطلوبة')}</span></div><div className="skill-groups"><section><h3><CheckCircle2/>{say('Demonstrated','مثبتة')}</h3>{groups.demonstrated.length?groups.demonstrated.map(x=><span key={x.skillId}>{skillCatalog[x.skillId].label[locale]}</span>):<small>{say('No strong claims yet','لا توجد ادعاءات قوية بعد')}</small>}</section><section><h3><Sparkles/>{say('Practiced','قيد التدريب')}</h3>{groups.practiced.length?groups.practiced.map(x=><span key={x.skillId}>{skillCatalog[x.skillId].label[locale]}</span>):<small>{say('Complete a case to begin','أكمل حالة للبدء')}</small>}</section><section><h3><Lock/>{say('Next to learn','المهارات التالية')}</h3>{groups.missing.slice(0,5).map(x=><span key={x.skillId}>{skillCatalog[x.skillId].label[locale]}</span>)}</section></div></article><aside><section className="career-next"><Map/><span>{say('NEXT CAREER MISSION','مهمة المسار التالية')}</span><h2>{ar?recommendation.ar:recommendation.en}</h2><p>{recommendation.skillId?say(`Recommended because ${skillCatalog[recommendation.skillId].label.en} is relevant to your target role.`,`موصى بها لأن ${skillCatalog[recommendation.skillId].label.ar} مطلوبة للدور المستهدف.`):say('Continue building diverse evidence.','استمر في بناء أدلة متنوعة.')}</p>{recommendation.route?<Link href={`/${locale}${recommendation.route}`}>{say('Open mission','افتح المهمة')}<Arrow/></Link>:<b><Lock/>{say('Locked until prior progression','مقفولة حتى إكمال التقدم السابق')}</b>}</section><section className="cv-live"><FileText/><span>{say('LIVE CV','السيرة التلقائية')}</span><h2>{say('Evidence-driven and role-targeted','مبنية على الأدلة وموجهة للدور')}</h2><p>{say('Practiced skills appear as development; Demonstrated skills can appear as core skills. Mizan Trading is labeled as simulation.','المهارات المتدرب عليها تظهر كتطوير، والمثبتة يمكن أن تظهر كمهارات أساسية. Mizan Trading موضحة كمحاكاة.')}</p><Link href={`/${locale}/career-profile/cv`}>{say('Open CV preview','افتح معاينة CV')}<Arrow/></Link></section></aside></section><section className="career-pipeline"><ShieldCheck/><div><b>GAMEPLAY → EVIDENCE → SKILL PASSPORT → ROLE → PROFILE → CV → EMPLOYER PREVIEW</b><small>{say(`${evidence.length} professional evidence records · local only · never Verified`,`${evidence.length} سجل دليل مهني · محلي فقط · غير موثق`)}</small></div><Link href={`/${locale}/career-profile/skills`}>{say('Inspect Skill Passport','افتح جواز المهارات')}<Arrow/></Link></section></main>}
