'use client';
import Link from'next/link';import{useEffect,useState}from'react';import{Check,Download,FileCheck2,Medal,ShieldCheck,Swords,Trophy,Users}from'lucide-react';import type{Locale}from'@/types';import{PlatformNav}from'./platform-nav';import{BrowserMultiProfileRepository}from'@/lib/local-competition/repository';import{BrowserCompetitionRepository}from'@/lib/local-competition/v2-repository';import{approvedChallenge,addCompetitionResult,beginDuel,duelComparison,emptyCompetition,gameAwards,leagueForProfile,recordDuel,scoreChallenge,streakDays,weeklyStandings,type ChallengeAnswer,type CompetitionChallenge,type CompetitionState}from'@/lib/local-competition/v2';import{createOfflineResult,localDuel,verifyOfflineResult}from'@/lib/local-competition/engine';import type{MultiProfileState,OfflineCompetitionResult}from'@/lib/local-competition/model';import'@/app/competition-v2.css';
const profileRepo=new BrowserMultiProfileRepository(),competitionRepo=new BrowserCompetitionRepository(),today=()=>new Date().toISOString().slice(0,10);type Tab='home'|'daily'|'duel'|'weekly'|'boss'|'profile'|'players'|'library';
const blank=():ChallengeAnswer=>({inspected:[],action:'investigate',debit:'',credit:'',amount:0,attempts:1});
export function LeaderboardPlatform({locale}:{locale:Locale}){const ar=locale==='ar',say=(en:string,a:string)=>ar?a:en,[profiles,setProfiles]=useState<MultiProfileState|null>(null),[state,setState]=useState<CompetitionState>(emptyCompetition),[tab,setTab]=useState<Tab>('home'),[challenge,setChallenge]=useState<CompetitionChallenge|null>(null),[mode,setMode]=useState<'daily'|'duel'>('daily'),[answer,setAnswer]=useState<ChallengeAnswer>(blank),[feedback,setFeedback]=useState(''),[opponentId,setOpponentId]=useState(''),[newName,setNewName]=useState(''),[newLevel,setNewLevel]=useState(2),[importText,setImportText]=useState(''),[imported,setImported]=useState<OfflineCompetitionResult|null>(null);useEffect(()=>{setProfiles(profileRepo.get());setState(competitionRepo.get())},[]);if(!profiles)return<main className="competition-v2">{say('Loading league…','جاري تحميل الدوري…')}</main>;const active=profiles.profiles.find(p=>p.id===profiles.activeProfileId)!,league=leagueForProfile(active),daily=approvedChallenge(today(),active),standings=weeklyStandings(profiles.profiles,state.results,league,today()),position=standings.findIndex(p=>p.profileId===active.id)+1,own=state.results.filter(p=>p.profileId===active.id),ranked=own.filter(p=>p.ranked),duel=state.duel,comparison=duel?duelComparison(duel):null,streak=streakDays(state.results,active.id),awards=gameAwards(state.results,active.id,duel);const save=(next:CompetitionState)=>{setState(next);competitionRepo.save(next)},start=(item:CompetitionChallenge,kind:'daily'|'duel')=>{setChallenge(item);setMode(kind);setAnswer(blank());setFeedback('');setTab(kind)},submit=()=>{if(!challenge)return;const result=scoreChallenge(challenge,answer,active,mode==='daily'||Boolean(duel?.ranked),new Date().toISOString());if(mode==='duel'&&duel){const next=recordDuel(duel,result);if(next===duel){setFeedback(say('Wrong duel participant','ليس هذا اللاعب المنتظر'));return}save({...addCompetitionResult(state,{...result,ranked:next.ranked}),duel:next});setChallenge(null);setFeedback(next.complete?say('Both players finished. Comparison unlocked.','انتهى اللاعبان. المقارنة متاحة.'):say('Player A completed. Score and answers are sealed until Player B finishes.','أنهى اللاعب الأول. الدرجة والإجابات مغلقة حتى ينتهي الثاني.'));return}const next=addCompetitionResult(state,result);save(next);setFeedback(next.results.at(-1)?.ranked?say(`Score ${result.score}/100 · local unverified`,`الدرجة ${result.score}/100 · محلية غير موثقة`):say('Practice replay · official score unchanged','إعادة تدريب · الدرجة الرسمية لم تتغير'));setChallenge(null)},switchProfile=(id:string)=>{profileRepo.switch(id);window.location.reload()},download=(name:string,content:string)=>{const url=URL.createObjectURL(new Blob([content],{type:'application/json'})),a=document.createElement('a');a.href=url;a.download=name;a.click();URL.revokeObjectURL(url)},exportResult=()=>{const row=ranked.at(-1);if(!row||!daily)return;const pkg=createOfflineResult({profileId:row.profileId,displayName:row.displayName,challenge:{...daily.seed,challengeId:row.challengeId,challengeVersion:row.challengeVersion,seed:row.seed},score:row.score*10,accuracy:row.dimensions.accuracy,investigation:row.dimensions.investigation,riskAwareness:row.dimensions.riskAwareness,assistanceUsed:0,completedAt:row.completedAt});download('career-league-result.json',JSON.stringify(pkg,null,2))},importResult=()=>{try{const value=JSON.parse(importText)as unknown;if(!verifyOfflineResult(value)||!daily||value.challenge.challengeId!==daily.seed.challengeId||value.challenge.challengeVersion!==daily.seed.challengeVersion||value.challenge.rulesetVersion!==daily.seed.rulesetVersion||value.challenge.seed!==daily.seed.seed)throw new Error('mismatch');setImported(value);setFeedback(say('Valid offline package · unverified comparison only','ملف صالح · مقارنة محلية غير موثقة فقط'))}catch{setImported(null);setFeedback(say('Invalid package or challenge version/seed mismatch','ملف غير صالح أو إصدار/بذرة التحدي مختلفة'))}};const menu:[Tab,string,string][]=[['home','Hub','الرئيسية'],['daily','Daily','اليومي'],['duel','Duel','مبارزة'],['weekly','Weekly','الأسبوعي'],['boss','Boss','الزعيم'],['profile','Profile','الملف'],['players','Players','اللاعبون'],['library','Library','المكتبة']];return<main className="competition-v2" dir={ar?'rtl':'ltr'}><PlatformNav locale={locale}/><section className="competition-shell"><header className="competition-hero"><div><span><Trophy/>{say('LOCAL CAREER LEAGUE · OFFLINE','الدوري المهني المحلي · بدون اتصال')}</span><h1>{say('Compete on accounting quality.','تنافس بجودة المحاسبة.')}</h1><p>{say('Same evidence, same scenario, fair local comparison. Not a live global leaderboard.','نفس الأدلة ونفس الحالة ومقارنة محلية عادلة. ليس ترتيبًا عالميًا مباشرًا.')}</p></div><aside><b>{active.displayName}</b><small>{leagueLabel(league,locale)}</small><strong>#{position||1}</strong><span>{standings[position-1]?.points??0} {say('weekly points','نقطة أسبوعية')}</span></aside></header><nav className="competition-tabs">{menu.map(([id,en,a])=><button key={id} className={tab===id?'active':''} onClick={()=>{setTab(id);setChallenge(null);setFeedback('')}}>{ar?a:en}</button>)}</nav>{feedback&&<p className="competition-feedback" role="status">{feedback}</p>}
{tab==='home'&&<div className="competition-cards"><article><Gamepad2Icon/><h2>{say('Daily Accounting Challenge','التحدي المحاسبي اليومي')}</h2><p>{daily?.title[locale]??say('Not available yet','غير متاح بعد')}</p><button disabled={!daily} onClick={()=>daily&&start(daily,'daily')}>{say('Play today','العب اليوم')}</button></article><article><Swords/><h2>{say('Local 1 vs 1','مبارزة محلية')}</h2><p>{say('Player A stays sealed until Player B completes the same case.','نتيجة اللاعب الأول مغلقة حتى يكمل الثاني نفس الحالة.')}</p><button onClick={()=>setTab('duel')}>{say('Set up duel','جهّز المبارزة')}</button></article><article><Medal/><h2>{say('Weekly League','الدوري الأسبوعي')}</h2><p>#{position||1} · {standings[position-1]?.points??0} {say('points','نقطة')}</p><button onClick={()=>setTab('weekly')}>{say('View standings','عرض الترتيب')}</button></article><article><ShieldCheck/><h2>{say('A win is not a skill.','الفوز ليس مهارة مهنية.')}</h2><p>{say('Points and badges are game identity. The Skill Passport uses qualifying case evidence only.','النقاط والشارات هوية لعب. جواز المهارات يستخدم دليل الحالات المؤهلة فقط.')}</p><Link href={`/${locale}/career-profile/skills`}>{say('Skill Passport','جواز المهارات')}</Link></article></div>}
{tab==='daily'&&!challenge&&<section className="competition-panel"><h2>{say('Today’s challenge','تحدي اليوم')}</h2>{daily?<><p>{daily.title[locale]} · {leagueLabel(league,locale)} · {today()}</p><p>{daily.approved?say('Approved First Shift case','حالة وردية أولى معتمدة'):say('Foundation practice preview · no official points','معاينة تدريب تأسيسي · بدون نقاط رسمية')}</p><button onClick={()=>start(daily,'daily')}>{say('Open case file','افتح ملف الحالة')}</button></>:<p>{say('Advanced content is locked until approved cases exist.','المحتوى المتقدم مقفل حتى تتوفر حالات معتمدة.')}</p>}</section>}
{(tab==='daily'||tab==='duel')&&challenge&&<section className="competition-case"><header><small>{mode==='duel'?say('SEALED DUEL CASE','حالة مبارزة مغلقة'):say('DAILY WORK FILE','ملف العمل اليومي')}</small><h2>{challenge.title[locale]}</h2><p>{challenge.context[locale]}</p><b>{challenge.amount.toLocaleString(locale)} EGP</b></header><div className="competition-docs"><h3>{say('Evidence documents','مستندات الأدلة')}</h3>{challenge.documents.map(doc=><button key={doc.id} className={answer.inspected.includes(doc.id)?'inspected':''} onClick={()=>setAnswer({...answer,inspected:[...new Set([...answer.inspected,doc.id])]})}><FileCheck2/><span><b>{doc.title[locale]}</b><small>{answer.inspected.includes(doc.id)?doc.summary[locale]:say('Open to inspect','افتح للفحص')}</small></span>{answer.inspected.includes(doc.id)&&<Check/>}</button>)}</div><div className="competition-decision"><h3>{say('Professional action & posting','القرار المهني والقيد')}</h3><label>{say('Decision','القرار')}<select value={answer.action} onChange={e=>setAnswer({...answer,action:e.target.value as ChallengeAnswer['action']})}><option value="investigate">{say('Investigate','حقق')}</option><option value="hold">{say('Hold','أوقف')}</option><option value="post">{say('Post after review','رحّل بعد الفحص')}</option></select></label><label>{say('Debit account','الحساب المدين')}<select value={answer.debit} onChange={e=>setAnswer({...answer,debit:e.target.value})}><option value="">—</option>{['equipment','bank','bank-charges','officeExpense','suppliers','customers','cash','revenue'].map(id=><option key={id} value={id}>{accountName(id,locale)}</option>)}</select></label><label>{say('Credit account','الحساب الدائن')}<select value={answer.credit} onChange={e=>setAnswer({...answer,credit:e.target.value})}><option value="">—</option>{['equipment','bank','bank-charges','officeExpense','suppliers','customers','cash','revenue'].map(id=><option key={id} value={id}>{accountName(id,locale)}</option>)}</select></label><label>{say('Amount','المبلغ')}<input type="number" min="0" value={answer.amount||''} onChange={e=>setAnswer({...answer,amount:Number(e.target.value)})}/></label><button onClick={submit}>{say('Submit professional decision','سلّم القرار المهني')}</button></div></section>}
{tab==='duel'&&!challenge&&<section className="competition-panel"><h2>{say('Local accounting duel','مبارزة محاسبية محلية')}</h2>{comparison&&duel?<div className="competition-vs"><h3>{say('DUEL RESULT · LOCAL UNVERIFIED','نتيجة المبارزة · محلية غير موثقة')}</h3><strong>{profiles.profiles.find(p=>p.id===duel.playerA)?.displayName} {comparison.scoreA} <span>VS</span> {comparison.scoreB} {profiles.profiles.find(p=>p.id===duel.playerB)?.displayName}</strong><p>{comparison.draw?say('Draw','تعادل'):comparison.winner===duel.playerA?say('Player A wins','فاز اللاعب الأول'):say('Player B wins','فاز اللاعب الثاني')}</p><div>{Object.entries(comparison.dimensionsA).map(([key,value])=><span key={key}>{key}: {value} / {comparison.dimensionsB[key as keyof typeof comparison.dimensionsB]}</span>)}</div></div>:duel?.resultA&&!duel.resultB?<div className="competition-sealed"><ShieldCheck/><h3>{say('Player A completed · result sealed','أنهى اللاعب الأول · النتيجة مغلقة')}</h3><p>{say('Switch to Player B. No answers or score are shown.','بدّل إلى اللاعب الثاني. لا تظهر إجابات أو درجة.')}</p><button disabled={active.id!==duel.playerB} onClick={()=>start(duel.challenge,'duel')}>{say('Play as Player B','العب باللاعب الثاني')}</button></div>:<><p>{say('Same league: ranked. Different tiers: unranked practice.','نفس الدوري: رسمية. مستويات مختلفة: تدريب غير مصنف.')}</p><label>{say('Opponent','الخصم')}<select value={opponentId} onChange={e=>setOpponentId(e.target.value)}><option value="">{say('Choose profile','اختر ملفًا')}</option>{profiles.profiles.filter(p=>p.id!==active.id).map(p=><option key={p.id} value={p.id}>{p.displayName} · {leagueLabel(leagueForProfile(p),locale)}</option>)}</select></label><button disabled={!opponentId||!daily} onClick={()=>{const other=profiles.profiles.find(p=>p.id===opponentId);if(!other||!daily)return;save({...state,duel:beginDuel(daily,active,other)});start(daily,'duel')}}>{say('Lock same challenge & begin','ثبت نفس التحدي وابدأ')}</button></>}</section>}
{tab==='weekly'&&<section className="competition-panel"><h2>{say('Weekly League','الدوري الأسبوعي')} · {leagueLabel(league,locale)}</h2><p>{say('Local profiles only; unique approved ranked attempts. No live players.','ملفات محلية فقط ومحاولات معتمدة فريدة. لا يوجد لاعبون مباشرون.')}</p><div className="competition-table"><header><span>#</span><span>{say('Player','اللاعب')}</span><span>{say('Points','النقاط')}</span><span>{say('Accuracy','الدقة')}</span><span>{say('Cases','الحالات')}</span></header>{standings.map((row,index)=><div key={row.profileId} className={row.profileId===active.id?'you':''}><b>{index+1}</b><span>{row.name}{row.profileId===active.id?` · ${say('YOU','أنت')}`:''}</span><strong>{row.points}</strong><span>{row.accuracy}%</span><span>{row.challenges}</span></div>)}</div><small>{say('Season 01 · local preview. Advanced tiers locked.','الموسم 01 · معاينة محلية. المستويات المتقدمة مقفلة.')}</small></section>}
{tab==='boss'&&<section className="competition-panel"><h2>{say('Boss events','أحداث الزعيم')}</h2><div className="competition-cards"><article><h3>{say('First Shift Boss','زعيم الوردية الأولى')}</h3><p>{say('Three related cases, protected posting and manager review.','ثلاث حالات مترابطة وترحيل محمي ومراجعة المدير.')}</p><Link href={`/${locale}/game/first-shift`}>{say('Enter full mission','ادخل المهمة الكاملة')}</Link></article><article><h3>{say('Bank Reconciliation Showdown','تحدي التسوية البنكية')}</h3><p>{say('Match, classify and prepare adjustments.','طابق وصنّف وجهّز التعديلات.')}</p><Link href={`/${locale}/game/bank-reconciliation`}>{say('Enter full mission','ادخل المهمة الكاملة')}</Link></article></div><small>{say('Boss completion is game-only; qualifying underlying cases produce professional evidence.','إتمام الزعيم مكافأة لعب؛ الحالات المؤهلة فقط تنتج دليلًا مهنيًا.')}</small></section>}
{tab==='profile'&&<section className="competition-panel"><h2>{say('Competition profile','ملف المنافسة')}</h2><p>{active.displayName} · {leagueLabel(league,locale)} · #{position||1}</p><div className="competition-stats"><span>{ranked.length}<small>{say('Ranked cases','حالات رسمية')}</small></span><span>{streak.current}<small>{say('Current streak','التتابع الحالي')}</small></span><span>{streak.best}<small>{say('Best streak','أفضل تتابع')}</small></span><span>{standings[position-1]?.points??0}<small>{say('Weekly points','نقاط الأسبوع')}</small></span></div><h3>{say('Game titles & badges · not qualifications','ألقاب وشارات اللعب · ليست مؤهلات')}</h3><div className="competition-awards">{awards.length?awards.map(value=><span key={value}><Medal/>{value}</span>):say('Earned from deterministic milestones.','تُكتسب من إنجازات محددة.')}</div><h3>{say('Recent matches','آخر المباريات')}</h3>{own.slice(-5).reverse().map(row=><p key={row.attemptId}>{row.caseId} · {row.score}/100 · {row.ranked?say('Ranked','رسمية'):say('Practice','تدريب')}</p>)}<div className="competition-offline"><h3>{say('Offline two-device preview','معاينة تنافس جهازين بدون اتصال')}</h3><button disabled={!ranked.length} onClick={exportResult}><Download/>{say('Export latest ranked result','صدّر آخر نتيجة رسمية')}</button><label>{say('Paste competitor JSON','ألصق JSON الخصم')}<textarea value={importText} onChange={e=>setImportText(e.target.value)} rows={4}/></label><button onClick={importResult}>{say('Validate & compare','تحقق وقارن')}</button>{imported&&ranked.length>0&&<p>{say('Imported competitor','الخصم المستورد')}: {imported.displayName} · {localDuel(createOfflineResult({profileId:ranked.at(-1)!.profileId,displayName:ranked.at(-1)!.displayName,challenge:imported.challenge,score:ranked.at(-1)!.score*10,accuracy:ranked.at(-1)!.dimensions.accuracy,investigation:ranked.at(-1)!.dimensions.investigation,riskAwareness:ranked.at(-1)!.dimensions.riskAwareness,assistanceUsed:0,completedAt:ranked.at(-1)!.completedAt}),imported).winnerProfileId??say('Draw','تعادل')} · {say('unverified','غير موثقة')}</p>}</div><p>{say('Winning never writes to Skill Passport or ATS CV.','الفوز لا يكتب في جواز المهارات أو السيرة الذاتية.')}</p></section>}
{tab==='players'&&<section className="competition-panel"><h2>{say('Local player profiles','ملفات اللاعبين المحلية')}</h2><p>{say('Switch isolates career, game, CV, First Shift and Bank Reconciliation saves. Duel summaries are shared for comparison only.','التبديل يعزل المسار واللعب والسيرة والوردية والتسوية. ملخص المبارزة للمقارنة فقط.')}</p>{profiles.profiles.map(row=><div className="competition-player" key={row.id}><Users/><span><b>{row.displayName}</b><small>{leagueLabel(leagueForProfile(row),locale)}</small></span><button disabled={row.id===active.id} onClick={()=>switchProfile(row.id)}>{say('Switch','بدّل')}</button><button onClick={()=>{const name=window.prompt(say('New name','الاسم الجديد'),row.displayName);if(name)setProfiles(profileRepo.rename(row.id,name))}}>{say('Rename','تسمية')}</button><button disabled={row.id===active.id||profiles.profiles.length<=1} onClick={()=>{if(window.confirm(say('Delete inactive profile?','حذف الملف غير النشط؟')))setProfiles(profileRepo.delete(row.id))}}>{say('Delete','احذف')}</button></div>)}<div className="competition-create"><input aria-label={say('Player name','اسم اللاعب')} value={newName} onChange={e=>setNewName(e.target.value)} placeholder={say('New player name','اسم لاعب جديد')}/><label>{say('Career simulation level','مستوى المحاكاة المهنية')}<select value={newLevel} onChange={e=>setNewLevel(Number(e.target.value))}>{[1,2,3,4,5].map(level=><option key={level} value={level}>{level} · {level===1?say('Student','طالب'):level<=3?say('Graduate / Junior','خريج / مبتدئ'):say('Accountant','محاسب')}</option>)}</select></label><button disabled={!newName.trim()} onClick={()=>{profileRepo.create({displayName:newName.trim(),persona:newLevel===1?'student':newLevel<=3?'graduate':'working-accountant',level:newLevel,track:'foundation'});window.location.reload()}}>{say('Create player','أنشئ لاعبًا')}</button><button onClick={()=>download('career-league-profiles.json',profileRepo.export())}>{say('Export profiles','تصدير الملفات')}</button><label>{say('Import profiles JSON','استيراد ملفات JSON')}<input type="file" accept="application/json" onChange={async e=>{const file=e.target.files?.[0];if(!file)return;try{profileRepo.import(await file.text());window.location.reload()}catch{setFeedback(say('Invalid profile package','ملف لاعبين غير صالح'))}}}/></label></div></section>}
{tab==='library'&&<section className="competition-panel"><h2>{say('Challenge library','مكتبة التحديات')}</h2><div className="competition-cards">{['supplier-invoice','customer-receipt','office-expense'].map(id=><article key={id}><h3>{id.replaceAll('-',' ')}</h3><p>{say('Approved First Shift case · Practical League','حالة وردية معتمدة · الدوري العملي')}</p><Link href={`/${locale}/game/first-shift`}>{say('Practice full case','تدرب على الحالة')}</Link></article>)}<article><h3>{say('Bank reconciliation','التسوية البنكية')}</h3><p>{say('Approved professional mission · General Accounting practice','مهمة مهنية معتمدة · تدريب المحاسبة العامة')}</p><Link href={`/${locale}/game/bank-reconciliation`}>{say('Open workpaper','افتح ورقة العمل')}</Link></article></div></section>}
<footer className="competition-footer">{say('Local Career League · editable device results are unverified. No online matchmaking or global ranking.','دوري محلي · النتائج القابلة للتعديل غير موثقة. لا توجد مطابقة عبر الإنترنت أو ترتيب عالمي.')}</footer></section></main>}
function Gamepad2Icon(){return<Swords/>}function leagueLabel(id:string,locale:Locale){const labels:Record<string,{en:string;ar:string}>={foundation:{en:'Foundation League',ar:'الدوري التأسيسي'},practical:{en:'Practical Accounting League',ar:'دوري المحاسبة العملية'},general:{en:'General Accounting League',ar:'دوري المحاسبة العامة'},'advanced-locked':{en:'Advanced Review · Locked',ar:'المراجعة المتقدمة · مقفل'},'leadership-locked':{en:'Finance Leadership · Locked',ar:'القيادة المالية · مقفل'}};return labels[id]?.[locale]??id}
function accountName(id:string,locale:Locale){const labels:Record<string,{en:string;ar:string}>={equipment:{en:'Equipment',ar:'المعدات'},bank:{en:'Bank',ar:'البنك'},'bank-charges':{en:'Bank charges expense',ar:'مصروف رسوم بنكية'},officeExpense:{en:'Office expense',ar:'مصروف مكتب'},suppliers:{en:'Suppliers',ar:'الموردون'},customers:{en:'Customers',ar:'العملاء'},cash:{en:'Cash',ar:'الصندوق'},revenue:{en:'Revenue',ar:'الإيراد'}};return labels[id]?.[locale]??id}
