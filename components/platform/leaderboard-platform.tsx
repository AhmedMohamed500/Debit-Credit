'use client';
import {useState} from 'react';import Link from 'next/link';import {ArrowLeft,ArrowRight,Clock3,Medal,ShieldCheck,Trophy,UserRound} from 'lucide-react';import {DemoLeaderboardRepository,leagueForScore,type CompetitionMode} from '@/lib/platform';import {careerLeagueScore} from '@/lib/career-league/engine';import {projectShiftPerformance} from '@/lib/cases/performance';import {firstDayProgress} from '@/lib/campaign/first-day';import {useGame} from '@/lib/campaign/store';import {usePlayer} from '@/components/game/use-player';import type {Locale} from '@/types';import {PlatformNav} from './platform-nav';
const repository=new DemoLeaderboardRepository();
export function LeaderboardPlatform({locale}:{locale:Locale}){const ar=locale==='ar',say=(en:string,a:string)=>ar?a:en,Arrow=ar?ArrowLeft:ArrowRight,[mode,setMode]=useState<CompetitionMode>('weekly'),{state}=useGame(),player=usePlayer(),performance=projectShiftPerformance(state),progress=firstDayProgress(state),raw=careerLeagueScore({...performance,firstAttemptQuality:progress.completed.length/3*100,difficulty:1,bossCompletion:progress.completed.length===3?100:0,consistency:progress.completed.length/3*100}),rows=repository.list(mode,{id:'local-player',nameAr:player.displayName,nameEn:player.displayName,score:raw,accuracy:performance.accuracy,investigation:performance.investigation,riskAwareness:performance.riskAwareness,demo:false}),position=rows.findIndex(x=>x.id==='local-player')+1,league=leagueForScore(raw),above=position>1?rows[position-2]:null,below=position>0&&position<rows.length?rows[position]:null,points=above?Math.max(0,above.score-rows[position-1].score):0;return <main className="platform-page leaderboard-page"><PlatformNav locale={locale}/><header className="leaderboard-hero"><div><span><Trophy/>{say('CAREER LEAGUE · LOCAL DEMO','CAREER LEAGUE · نسخة محلية تجريبية')}</span><h1>{say('Junior Accountant League','دوري المحاسب المبتدئ')}</h1><p>{say('Professional-quality ranking with deterministic demo competitors. No real players are online.','ترتيب لجودة الأداء مع منافسين تجريبيين ثابتين. لا يوجد لاعبون حقيقيون متصلون.')}</p></div><aside><Medal/><strong>{league}</strong><span>#{position||rows.length}</span><small>{above?`${points} ${say('pts behind','نقطة خلف')} #${position-1}`:say('Top position','المركز الأول')}</small><small>{say('Above','أعلى')}: {above?.nameEn??'—'} · {say('Below','أسفل')}: {below?.nameEn??'—'}</small></aside></header><section className="score-formula"><ShieldCheck/><div><b>{say('Professional performance score','نقاط الأداء المهني')}</b><p>{say('Accuracy, judgment, investigation, risk awareness, first-attempt quality, difficulty, boss completion, and consistency. Speed has no direct reward.','الدقة والحكم والتحقيق والوعي بالمخاطر وجودة المحاولة الأولى والصعوبة ومهمات Boss والاستمرارية. السرعة لا تمنح نقاطًا مباشرة.')}</p></div></section><div className="leaderboard-tabs">{(['weekly','season','all-time']as CompetitionMode[]).map(id=><button className={mode===id?'active':''} onClick={()=>setMode(id)} key={id}>{id==='weekly'?say('Weekly','أسبوعي'):id==='season'?say('Closing Season','موسم الإقفال'):say('All time','كل الوقت')}</button>)}</div><section className="leaderboard-table"><header><span>#</span><span>{say('Player','اللاعب')}</span><span>{say('Quality','الجودة')}</span><span>{say('Accuracy','الدقة')}</span><span>{say('Investigation','التحقيق')}</span><span>{say('Risk','المخاطر')}</span></header>{rows.map((row,i)=><article className={row.id==='local-player'?'you':''} key={row.id}><strong>{i+1}</strong><div><UserRound/><span><b>{ar?row.nameAr:row.nameEn}</b><small>{row.demo?say('DEMO COMPETITOR','منافس تجريبي'):say('YOU · LOCAL','أنت · محلي')}</small></span></div><b>{row.score}</b><span>{row.accuracy}%</span><span>{row.investigation}%</span><span>{row.riskAwareness}%</span></article>)}</section><section className="season-card"><Clock3/><div><span>{say('CURRENT SAMPLE EVENT','الحدث التجريبي الحالي')}</span><h2>{say('Supplier Crisis','أزمة المورد')}</h2><p>{say('A local Career Season sample using the existing supplier investigation. Rotating online seasons require future backend infrastructure.','عينة موسم محلية تستخدم تحقيق المورد الحالي. المواسم المتغيرة عبر الإنترنت تحتاج بنية خلفية مستقبلية.')}</p></div><Link href={`/${locale}/game/first-shift`}>{say('Enter event','ادخل الحدث')}<Arrow/></Link></section></main>}
