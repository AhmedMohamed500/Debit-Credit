'use client';
import{useState}from'react';import Link from'next/link';import{ArrowLeft,ArrowRight,Clock3,Medal,ShieldCheck,Trophy,UserRound}from'lucide-react';import{competitionScore,DemoLeaderboardRepository,leagueForScore,type CompetitionMode}from'@/lib/platform';import{projectShiftPerformance}from'@/lib/cases/performance';import{firstDayProgress}from'@/lib/campaign/first-day';import{useGame}from'@/lib/campaign/store';import{usePlayer}from'@/components/game/use-player';import type{Locale}from'@/types';import{PlatformNav}from'./platform-nav';
const repository=new DemoLeaderboardRepository();
export function LeaderboardPlatform({locale}:{locale:Locale}){const ar=locale==='ar',say=(en:string,a:string)=>ar?a:en,Arrow=ar?ArrowLeft:ArrowRight,[mode,setMode]=useState<CompetitionMode>('weekly'),{state}=useGame(),player=usePlayer(),performance=projectShiftPerformance(state),progress=firstDayProgress(state),raw=competitionScore({...performance,firstAttemptRate:progress.completed.length/3*100}),rows=repository.list(mode,{id:'local-player',nameAr:player.displayName,nameEn:player.displayName,score:raw,accuracy:performance.accuracy,investigation:performance.investigation,riskAwareness:performance.riskAwareness,demo:false}),position=rows.findIndex(x=>x.id==='local-player')+1,league=leagueForScore(raw);return <main className="platform-page leaderboard-page"><PlatformNav locale={locale}/><header className="leaderboard-hero"><div><span><Trophy/>{say('LOCAL COMPETITION PREVIEW','معاينة منافسة محلية')}</span><h1>{say('Accounting quality league','دوري جودة المحاسبة')}</h1><p>{say('Ranked by accuracy, judgment, investigation, risk awareness, and first-attempt quality. Demo competitors are deterministic sample profiles, not real players.','الترتيب يعتمد على الدقة والحكم والتحقيق والوعي بالمخاطر وجودة المحاولة الأولى. المنافسون بيانات تجريبية ثابتة وليسوا لاعبين حقيقيين.')}</p></div><aside><Medal/><strong>{league}</strong><span>#{position||rows.length}</span></aside></header><section className="score-formula"><ShieldCheck/><div><b>{say('Competition Score is explainable','نقاط المنافسة قابلة للتفسير')}</b><p>Accuracy × 3 + Judgment × 2.5 + Investigation × 1.8 + Risk × 1.8 + First Attempt × 0.6 + Difficulty</p></div></section><div className="leaderboard-tabs">{(['weekly','season','all-time']as CompetitionMode[]).map(id=><button className={mode===id?'active':''} onClick={()=>setMode(id)} key={id}>{id==='weekly'?say('Weekly','أسبوعي'):id==='season'?say('Season','الموسم'):say('All time','كل الوقت')}</button>)}</div><section className="leaderboard-table"><header><span>#</span><span>{say('Player','اللاعب')}</span><span>{say('Quality','الجودة')}</span><span>{say('Accuracy','الدقة')}</span><span>{say('Investigation','التحقيق')}</span><span>{say('Risk','المخاطر')}</span></header>{rows.map((row,i)=><article className={row.id==='local-player'?'you':''} key={row.id}><strong>{i+1}</strong><div><UserRound/><span><b>{ar?row.nameAr:row.nameEn}</b><small>{row.demo?say('DEMO COMPETITOR','منافس تجريبي'):say('YOU · LOCAL','أنت · محلي')}</small></span></div><b>{row.score}</b><span>{row.accuracy}%</span><span>{row.investigation}%</span><span>{row.riskAwareness}%</span></article>)}</section><section className="season-card"><Clock3/><div><span>{say('CURRENT CHALLENGE','التحدي الحالي')}</span><h2>{say('Supplier Investigation','تحقيق المورد')}</h2><p>{say('Complete the supplier case with full evidence before posting. Speed has only a minor role; unsafe rushing reduces the result.','أكمل حالة المورد مع فحص الأدلة قبل الترحيل. السرعة عامل صغير والتسرع غير الآمن يخفض النتيجة.')}</p></div><Link href={`/${locale}/game/first-shift`}>{say('Enter challenge','ادخل التحدي')}<Arrow/></Link></section></main>}
