import{roleCatalog,skillCatalog}from'./catalog';import type{CareerProfile,CvDocument}from'./model';
export interface AtsFormatCheck{id:'single-column'|'standard-headings'|'contact'|'duplicate-bullets'|'bullet-length'|'selectable-text'|'supported-keywords';passed:boolean;message:string}
export function cvToPlainText(profile:CareerProfile,cv:CvDocument){const lines=[profile.fullName,cv.headline,[profile.email,profile.phone,profile.location,profile.country].filter(Boolean).join(' | '),'','PROFESSIONAL SUMMARY',cv.summary,'','TARGET ROLE',roleCatalog[cv.targetRoleId].label.en,'','CORE ACCOUNTING SKILLS',...cv.coreSkills.map(item=>skillCatalog[item.skillId].label.en),...cv.practiceSkills.length?['','ACCOUNTING PRACTICE',...cv.practiceSkills.map(item=>skillCatalog[item.skillId].label.en)]:[],...cv.simulationBullets.length?['','ACCOUNTING SIMULATION EXPERIENCE','Mizan Trading — Practical Simulation',...cv.simulationBullets.map(item=>`• ${item.text}`)]:[],...profile.education.length?['','EDUCATION',...profile.education.map(item=>`${item.degree}${item.field?` — ${item.field}`:''} | ${item.institution}${item.graduationYear?` | ${item.graduationYear}`:''}`)]:[],...profile.experience.length?['','EXPERIENCE',...profile.experience.map(item=>`${item.title} | ${item.company}\n${item.description}`)]:[],...profile.languages.length?['','LANGUAGES',profile.languages.join(' | ')]:[]];return lines.join('\n').replace(/\n{3,}/g,'\n\n').trim()}
export function checkAtsFormat(profile:CareerProfile,cv:CvDocument):AtsFormatCheck[]{const bullets=cv.simulationBullets.map(item=>item.text.trim()),normalized=bullets.map(item=>item.toLowerCase()),keywords=new Set([...cv.coreSkills,...cv.practiceSkills].map(item=>skillCatalog[item.skillId].label.en.toLowerCase()));return[{id:'single-column',passed:true,message:'Generated CV uses one reading column.'},{id:'standard-headings',passed:cv.sections.includes('contact')&&cv.sections.includes('summary')&&cv.sections.includes('skills'),message:'Standard contact, summary and skills headings are present.'},{id:'contact',passed:Boolean(profile.fullName&&profile.email),message:'Full name and email are present.'},{id:'duplicate-bullets',passed:new Set(normalized).size===normalized.length,message:'Simulation bullets are unique.'},{id:'bullet-length',passed:bullets.every(item=>item.length<=220),message:'Simulation bullets stay within 220 characters.'},{id:'selectable-text',passed:true,message:'Export is text, not a flattened image.'},{id:'supported-keywords',passed:keywords.size>0,message:'Keywords come from evidence-backed skills or saved experience.'}]}
