import{roleCatalog}from'./catalog';import type{CareerProfile,CvDocument,RoleId,SkillEvidence,SkillResult}from'./model';
export function buildCv(profile:CareerProfile,passport:SkillResult[],evidence:SkillEvidence[],targetRoleId:RoleId):CvDocument{const weights=roleCatalog[targetRoleId].skills,skills=passport.filter(s=>s.status!=='unassessed').sort((a,b)=>(weights[b.skillId]??0)-(weights[a.skillId]??0)||(b.score??0)-(a.score??0)),relevant=evidence.filter(e=>e.accuracy>=70&&e.roleRelevance.includes(targetRoleId)).sort((a,b)=>b.completedAt.localeCompare(a.completedAt)),role=roleCatalog[targetRoleId].label;return{targetRoleId,headline:profile.headline||role.en,summary:profile.summary||`Accounting candidate targeting ${role.en}, with practical simulation evidence in ${skills.slice(0,3).map(s=>s.skillId.replaceAll('-',' ')).join(', ')||'developing accounting skills'}.`,skills,evidence:relevant,sections:['contact','summary','target-role','education','experience','skills','simulations','evidence','languages','links']}}
export function exportCareerData(profile:CareerProfile,cv:CvDocument,evidence:SkillEvidence[]){return JSON.stringify({schema:'debit-credit-career-export-v1',exportedAt:new Date().toISOString(),profile,cv,evidence},null,2)}
