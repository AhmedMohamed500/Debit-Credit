import{connectedMissionGraph}from'./catalog';import type{CompanyProgressionState}from'@/lib/company-progression/model';import type{ProjectedMissionConsequence}from'./model';
export function projectConnectedConsequences(state:CompanyProgressionState,targetMissionId?:string):ProjectedMissionConsequence[]{return connectedMissionGraph.filter(edge=>state.outcomes[edge.sourceMissionId]===edge.sourceOutcome&&(!targetMissionId||edge.targetMissionId===targetMissionId)).map(edge=>({edgeId:edge.id,targetMissionId:edge.targetMissionId,kind:edge.kind,description:edge.description,value:edge.value??null,flag:edge.flag}))}
export function connectedMissionReadiness(state:CompanyProgressionState,missionId:string){const consequences=projectConnectedConsequences(state,missionId),blockers=consequences.filter(item=>item.kind==='close-blocker'),openControls=consequences.filter(item=>item.kind==='control'&&!item.flag.endsWith('cleared'));return{ready:blockers.length===0&&openControls.length===0,blockers:[...blockers,...openControls],context:consequences}}
export function connectedBalanceEffect(state:CompanyProgressionState,missionId:string){return projectConnectedConsequences(state,missionId).filter(item=>item.kind==='balance').reduce((total,item)=>total+(item.value??0),0)}
