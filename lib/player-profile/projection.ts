import{roleCatalog,skillCatalog}from'@/lib/career/catalog';import type{CareerProfile,SkillEvidence,SkillResult}from'@/lib/career/model';import type{PlacementResult}from'@/lib/placement/model';
export interface PlayerCareerIdentity{displayName:string;targetRole:string;simulationLevel:string;professionalStrengths:string[];developmentAreas:string[];completedCases:number;verifiedAssessmentAvailable:false;evidenceLabel:'Local practical simulation';gameMetricsExcluded:true}
export function buildPlayerCareerIdentity(profile:CareerProfile,passport:SkillResult[],evidence:SkillEvidence[],placement:PlacementResult|null):PlayerCareerIdentity{const assessed=passport.filter(item=>item.status!=='unassessed'),strengths=assessed.filter(item=>item.status==='demonstrated'||item.status==='verified').sort((a,b)=>(b.score??0)-(a.score??0)).slice(0,5),developing=assessed.filter(item=>item.status==='practiced').sort((a,b)=>(b.score??0)-(a.score??0)).slice(0,5),simulationLevel=profile.experienceLevel==='student'?'Accounting Student':profile.experienceLevel==='fresh-graduate'?'Graduate / Trainee':profile.experienceLevel==='junior'?'Junior Accountant Practice':profile.experienceLevel==='mid'?'Accountant Practice':'Senior Practice';const placementGaps=placement?.gaps.map(area=>area.replaceAll('-',' '))??[];return{displayName:profile.fullName,targetRole:roleCatalog[profile.targetRoleId].label.en,simulationLevel,professionalStrengths:strengths.map(item=>skillCatalog[item.skillId].label.en),developmentAreas:[...developing.map(item=>skillCatalog[item.skillId].label.en),...placementGaps].slice(0,6),completedCases:new Set(evidence.filter(item=>item.accuracy>=70).map(item=>item.activityId)).size,verifiedAssessmentAvailable:false,evidenceLabel:'Local practical simulation',gameMetricsExcluded:true}}
