import{describe,expect,it}from'vitest';import{companyEvolution,companyEvolutionIsProgressive}from'@/lib/advanced-companies';import{companyTiers}from'@/lib/career-league/catalog';
describe('advanced company progression',()=>{it('maps every existing Career League company exactly once',()=>{expect(companyEvolution.map(item=>item.id)).toEqual(companyTiers.map(item=>item.id))});it('increases structure, ambiguity, controls, English, review and deadline pressure at every tier',()=>{expect(companyEvolutionIsProgressive()).toBe(true)});it('does not present planned advanced companies as implemented',()=>{expect(companyEvolution.filter(item=>item.tier>=4).every(item=>item.status==='planned')).toBe(true)})});
