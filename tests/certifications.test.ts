import{describe,expect,it}from'vitest';import{certificationPreparationTracks}from'@/lib/certifications';
describe('professional certification preparation roadmap',()=>{it('keeps every track planned and explicitly unofficial',()=>{expect(certificationPreparationTracks).toHaveLength(4);expect(certificationPreparationTracks.every(item=>item.status==='planned'&&!item.official&&!item.accredited&&!item.partner)).toBe(true)});it('uses preparation, readiness or practice labels only',()=>{expect(certificationPreparationTracks.every(item=>['Preparation Track','Readiness Track','Practice Track'].includes(item.label))).toBe(true)})});
