import { describe,expect,it } from "vitest";
import { learningLevels, skillLabels } from "@/data/game";
describe("game localization and direction contracts",()=>{it("has professional Arabic and English copy for every level and step",()=>{expect(learningLevels).toHaveLength(8);for(const level of learningLevels){expect(level.titleAr).toBeTruthy();expect(level.titleEn).toBeTruthy();expect(level.steps.every(step=>step.titleAr&&step.titleEn&&step.route)).toBe(true)}});it("has Arabic and English labels for every skill",()=>{expect(Object.values(skillLabels).every(label=>label.ar&&label.en)).toBe(true)});it("maps locale to the expected document direction",()=>{const direction=(locale:string)=>locale==="ar"?"rtl":"ltr";expect(direction("ar")).toBe("rtl");expect(direction("en")).toBe("ltr")})});
