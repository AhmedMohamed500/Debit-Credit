import{describe,expect,it}from'vitest';import{standardsEligibleForAssessment,standardsRoadmap}from'@/lib/standards';
describe('Accounting Standards World roadmap',()=>{it('requires a reference, effective version and review before assessment',()=>{expect(standardsRoadmap.every(item=>item.reference)).toBe(true);expect(standardsEligibleForAssessment()).toEqual([])});it('labels all current content as preview or review required',()=>{expect(standardsRoadmap.every(item=>item.status!=='approved'&&item.professionalAssessment===false)).toBe(true)});it('covers framework, presentation, recognition and disclosure architecture',()=>{expect(new Set(standardsRoadmap.map(item=>item.area))).toEqual(new Set(['framework','presentation','recognition','disclosure','standard']))})});
